on: [push]

jobs:
  generate_readme:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Add a wip gif
        uses: rocktimsaikia/wip-action-gif@master
        with:
          GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
