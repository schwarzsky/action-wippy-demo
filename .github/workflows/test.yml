on: [push]

jobs:
  generate_readme:
    runs-on: ubuntu-latest
    steps:
      - name: work-in-progress gif
        uses: rocktimsaikia/action-wippy@master
        with:
          GITHUB_TOKEN: ${{ secrets.MY_GITHUB_TOKEN }}
